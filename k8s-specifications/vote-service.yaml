apiVersion: v1
kind: Service
metadata:
  name: vote-active
spec:
  type: NodePort
  selector:
    app: vote
  ports:
  - port: 80
    targetPort: 80
    nodePort: 31000
---
apiVersion: v1
kind: Service
metadata:
  name: vote-preview
spec:
  selector:
    app: vote
  ports:
  - port: 80
    targetPort: 80
